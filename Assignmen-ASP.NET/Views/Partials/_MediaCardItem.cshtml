<div class="col">
    <div class="card">
        <img src="@Model.ImageUrl" alt="@Model.Title">
        <div class="card-body">
            <h1>@Model.Title</h1>
            <p class="ingress">@Model.Ingress</p>
            <p>@Model.Stamp</p>
        </div>
    </div>
</div>